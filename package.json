{
  "name": "astra",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "client": "npm run dev --prefix client",
    "test": "npm run test --prefix client"
  }
}
